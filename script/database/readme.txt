//H2 database
http://localhost:8080/h2-console

url=jdbc:h2:mem:testdb
username=sa
password=password

 create table country (
        id integer generated by default as identity,
        population bigint,
        country_name varchar(255),
        primary key (id)
    )

INSERT INTO COUNTRY (POPULATION,COUNTRY_NAME) VALUES(10000, 'Italy');
INSERT INTO COUNTRY (POPULATION,COUNTRY_NAME) VALUES(20000, 'France');
INSERT INTO COUNTRY (POPULATION,COUNTRY_NAME) VALUES(30000, 'Germany');

select * from COUNTRY

